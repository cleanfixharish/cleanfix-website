# Atoms Minimum-Credit Synchronization Prompt

Use the existing CleanFixHarish project. Do not create a new project.

Canonical repository:
`https://github.com/cleanfixharish/cleanfix-website`

Canonical branch:
`main`

Instructions:

1. Synchronize the existing Atoms project with the current `main` branch from the canonical repository.
2. Preserve all existing work. Do not rebuild the application and do not replace the project structure.
3. Do not generate or rewrite website content, photographs, logos, icons, backgrounds, or dashboard UI.
4. Do not add or replace authentication, databases, hosting, CRM systems, plugins, libraries, or frameworks.
5. Do not upgrade packages or spend image, computer, browser, API, or build credits.
6. Do not deploy, change DNS, create paid resources, or enable paid services.
7. First inspect only. Confirm that the manager dashboard exists at:
   `app/frontend/src/pages/AdminPage.tsx`
8. Confirm that the dashboard starter data exists at:
   `app/frontend/src/data/adminStarterData.ts`
9. Confirm that the brand assets exist at:
   `app/frontend/public/assets/brand/`
10. Confirm that the local website images exist at:
    `app/frontend/public/assets/images/`
11. Run only the existing standard validation required by Atoms to confirm compatibility.
12. If an Atoms-specific incompatibility exists, stop and report:
    - the exact file
    - the exact error
    - the smallest proposed fix
    - the estimated credit impact
13. Do not make any fix until I approve it.

Return only a short synchronization and compatibility report. Do not begin a new build task.
